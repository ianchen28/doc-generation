"""
大纲加载器提示词模块
用于处理用户上传的大纲文件
"""

# 默认版本的大纲加载器提示词
V1_DEFAULT_PROMPT = """你是一个专业的文档大纲分析专家。用户提供了一个大纲文件，你需要将其转换为标准的结构化大纲格式。

请分析用户提供的大纲文件内容，并生成一个符合以下JSON格式的标准大纲：

```json
{
  "title": "文档标题",
  "description": "文档描述",
  "chapters": [
    {
      "title": "章节标题",
      "description": "章节描述",
      "sections": [
        {
          "title": "小节标题",
          "description": "小节描述"
        }
      ]
    }
  ]
}
```

要求：
1. 保持用户原始大纲的结构和逻辑
2. 确保章节和小节的层次关系清晰
3. 为每个章节和小节添加合适的描述
4. 如果用户提供了字数要求，请确保大纲能够支持该字数要求
5. 如果用户提供了样式指南或需求文档，请参考这些要求

用户主题：{topic}
用户大纲文件内容：
{outline_content}

请生成标准格式的大纲JSON："""

# 所有可用的提示词版本
PROMPTS = {
    "v1_default": V1_DEFAULT_PROMPT,
}
